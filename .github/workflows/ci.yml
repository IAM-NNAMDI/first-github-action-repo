name: Github action usage

on: push
jobs:
   Action-demo:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: printing index.html
        run: cat index.html
