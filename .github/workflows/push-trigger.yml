name: Push trigger demo
on: 
 push:
  branches:
    - main
    - dev
 pull_request:
  branches:
   - main
jobs:
  Job-1:
    runs-on: ubuntu-latest
    steps:
      - name: Build stage
        run: echo "Building the application"